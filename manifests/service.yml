apiVersion: v1
kind: Service
metadata:
    name: "antonmarkov-dbc-spa"
    labels:
        app: "antonmarkov-dbc-spa"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "antonmarkov-dbc-spa"